import ToolLayoutWrapper from '@/components/tools/ToolLayoutWrapper'

export default function ToolsAreaLayout({ children }: { children: React.ReactNode }) {
  return <ToolLayoutWrapper>{children}</ToolLayoutWrapper>
}
